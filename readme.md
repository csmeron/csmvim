# Introduction
---
This repo is the host/storage for my Neovim configuration, dearly named CSMVim.
I had one that I used to use, but it was messy and ugly, so I am remaking a config from scratch.
As I like its style, this config is heavily inspired by the appearance of LazyVim.

As with most nvim configs, `init.lua` is the config entry point.

